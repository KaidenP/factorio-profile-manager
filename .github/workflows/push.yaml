on:
  push:
    branches:
      - '*'

jobs:
  build:
    name: Build and upload commit
    runs-on: windows-latest
    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v6

      - name: Delete existing nightly release
        shell: cmd
        run: |
          gh release delete nightly --yes || ver>nul
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete existing nightly tag
        shell: cmd
        run: |
          git tag -d nightly || ver>nul
          git push origin :refs/tags/nightly 2>$null || ver>nul

      - name: Setup Node.js
        id: setup-node
        uses: actions/setup-node@v6
        with:
          node-version: latest
          cache: yarn

      - name: Initialize Workspace
        id: init-node
        run:
          yarn run init

      - name: Build
        id: build
        run:
          yarn run build

      - name: Create Setup
        id: build-setup
        uses: Minionguyjpro/Inno-Setup-Action@v1.2.2
        with:
          path: setup.iss

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
        with:
          tag_name: nightly
          release_name: Most Recent Commit
          body: |
            # Commit on ${{ github.ref_name }}
            ${{ github.event.commits[0].message }}
          draft: false
          prerelease: true

      - name: Upload Setup
        id: upload-setup
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
          asset_path: dist/Factorio Profile Manager Setup.exe
          asset_name: "Factorio Profile Manager Setup.exe"
          asset_content_type: application/octet-stream

      - name: Upload Portable
        id: upload-portable
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
          asset_path: dist/factorio-profile-manager-release.zip
          asset_name: "Factorio Profile Manager Portable.zip"
          asset_content_type: application/octet-stream